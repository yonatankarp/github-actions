name: Continuous Integration

on:
  pull_request:
    types: [ opened, synchronize, reopened, ready_for_review ]

concurrency:
  group: ${{ github.workflow }}-${{ github.job }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  pipeline:
    name: "[ test ] pipeline"
    strategy:
      fail-fast: false
      matrix:
        include:
          - variant: spring-skeleton
            image-name: spring-skeleton
            dockerfile-path: skeletons/spring/Dockerfile
            gradle-module: ''
          - variant: ktor-skeleton-uppercase
            image-name: KTOR-SKELETON
            dockerfile-path: skeletons/ktor/Dockerfile
            gradle-module: ''
          - variant: spring-skeleton-no-docker
            image-name: spring-skeleton
            dockerfile-path: ''
            gradle-module: ''
          - variant: spring-skeleton-submodule
            image-name: spring-skeleton
            dockerfile-path: skeletons/spring/Dockerfile
            gradle-module: ':skeletons:spring'
    uses: ./.github/workflows/ci.yml
    with:
      image-name: ${{ matrix.image-name }}
      dockerfile-path: ${{ matrix.dockerfile-path }}
      gradle-module: ${{ matrix.gradle-module }}

  dependabot_auto_merge:
    needs: pipeline
    if: ${{ github.actor == 'dependabot[bot]' }}
    uses: ./.github/workflows/dependabot-auto-merge.yml
    permissions:
      contents: write
      pull-requests: write
    with:
      auto-update-tag: true
    secrets:
      GITHUB_PAT: ${{ secrets.CI_PAT }}

  linters:
    uses: ./.github/workflows/linters.yml

  update-gradle-wrapper:
    uses: ./.github/workflows/update-gradle-wrapper.yml
    secrets:
      GITHUB_PAT: ${{ secrets.CI_PAT }}

  actions-linter:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v6

      - uses: devops-actions/actionlint@v0.1.12

  all-green:
    needs:
      - pipeline
      - linters
      - update-gradle-wrapper
      - actions-linter
    if: always()
    runs-on: ubuntu-latest
    steps:
      - uses: re-actors/alls-green@release/v1
        with:
          jobs: ${{ toJSON(needs) }}
